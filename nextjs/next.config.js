/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: "/239iwofk",
  images: {
    domains: ["https://yts.mx/"],
    unoptimized: true,
  },
};

module.exports = nextConfig;
